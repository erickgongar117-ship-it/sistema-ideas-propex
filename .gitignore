node_modules
.next
.env
*.db
*.db-journal
public/uploads/*
!public/uploads/.gitkeep
exports
power-platform-migration/export/
*.log
*.tsbuildinfo

.vercel
.env*

# --- agregado 2026-08-15: artefactos que ensuciaban `git status` ---
# Sin esto, `git status` mostraba 48 pendientes y escondia el codigo que si faltaba
# guardar. Un `git add -A` habria intentado subir ~900 MB al repo.

# Variantes de build de Next
.next-*/

# Trabajo temporal y salidas de herramientas
tmp/
outputs/
.propex_excel_work/
.qa_manual/

# Repositorio git independiente: es otro proyecto (Cloudflare/wrangler, 737 MB, 30 node_modules).
# No debe vivir anidado aqui; conviene moverlo fuera del repo cuando se pueda.
propex-interno-sites/

# Instantanea del codigo fuente: redundante, para eso esta git
PROPEX_SOURCE_SNAPSHOT.zip

# Volcados de inspeccion de documentos
*.inspect.ndjson

# Documentos GENERADOS: se reconstruyen con build_manual_propex.py, que si esta versionado.
# Fuera del repo porque git guarda una copia completa de cada version de un binario:
# reeditar el manual veinte veces serian ~13 MB permanentes en el historial.
Manual_de_Usuario_e_Instructivo_PROpEx.docx
Capacitacion_PROpEx_Flujo_y_Roles.pptx

# No versionada a proposito (ver CLAUDE.md, "Flujo seguro de trabajo compartido")
src/app/calculadora-pollos/
